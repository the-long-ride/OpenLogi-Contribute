[package]
name = "openlogi-gui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "GPUI-based desktop UI for OpenLogi."
default-run = "openlogi-gui"
# Not publishable to crates.io: depends on git-only gpui / gpui_platform
# (gpui_platform is not on the registry). Distributed as a bundled .app.
publish = false

[[bin]]
name = "openlogi-gui"
path = "src/main.rs"

[[bin]]
name = "openlogi-overlay"
path = "src/bin/openlogi-overlay.rs"

[dependencies]
openlogi-core = { path = "../openlogi-core" }
openlogi-camera = { path = "../openlogi-camera" }
openlogi-hook = { path = "../openlogi-hook" }
openlogi-assets = { path = "../openlogi-assets" }
openlogi-ipc = { path = "../openlogi-ipc" }
tarpc = { workspace = true }
gpui = { workspace = true }
gpui_platform = { workspace = true }
gpui-component = { workspace = true }
gpui-component-assets = { workspace = true }
tokio = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
thiserror.workspace = true
ureq.workspace = true
serde = { workspace = true }
serde_json = { workspace = true }
image = { version = "0.25", default-features = false, features = ["png"] }
rust-i18n = "4"
sys-locale = "0.3.2"
gpui-updater = { git = "https://github.com/AprilNEA/gpui-updater", tag = "v0.0.7", features = ["gpui"] }
# Spawn the agent under its own macOS TCC identity (see ipc_client::spawn_agent).
# Cross-platform: a no-op pass-through to std::process::Command off macOS.
disclaim = "0.1"
url = "2.5.8"
walkdir = "2.5.0"
fluent-langneg = "0.14.2"
backon.workspace = true
opener = { workspace = true }
succession = "0.1"

# Windows exe resources (app icon + VERSIONINFO) — see build.rs. The exact
# version already in Cargo.lock as gpui's own build-dependency, so the resolve
# adds an edge, not a crate, and the pinned gpui rev cannot move.
[build-dependencies]
embed-resource = "3.0.9"

# The Actions Ring overlay keeps one native window warm where the windowing
# backend exposes safe hide/show/reposition primitives. raw-window-handle lets
# the Windows and X11 implementations identify GPUI's existing native window.
[target.'cfg(any(target_os = "windows", target_os = "linux"))'.dependencies]
raw-window-handle = "0.6.2"

[target.'cfg(target_os = "windows")'.dependencies]
windows-sys = { workspace = true, features = [
    "Win32_Foundation",
    "Win32_Graphics_Gdi",
    "Win32_UI_WindowsAndMessaging",
] }

[target.'cfg(target_os = "linux")'.dependencies]
x11rb = "0.13.2"

# Menu-bar (NSStatusItem) FFI — GPUI has no status-bar API. objc2 gives typed,
# `Retained<T>`-owned AppKit bindings so the status-item code can't leak (#99).
[target.'cfg(target_os = "macos")'.dependencies]
# CGGetActiveDisplayList/CGDisplayBounds for the overlay: locating the cursor's
# display needs global display bounds, which GPUI's display API doesn't expose
# (its bounds are display-relative). C FFI, so core-graphics per platform/AGENTS.md.
core-graphics = { workspace = true }
core-foundation = { workspace = true }
objc2 = { workspace = true }
objc2-app-kit = { workspace = true, features = ["NSStatusBar", "NSStatusItem", "NSStatusBarButton", "NSButton", "NSControl", "NSResponder", "NSView", "NSMenu", "NSMenuItem", "NSImage", "NSApplication", "NSAppearance", "NSWindow", "NSGraphics", "NSWorkspace", "NSRunningApplication", "NSEvent", "NSScreen", "block2", "libc"] }
objc2-foundation = { workspace = true, features = ["NSString", "NSProcessInfo"] }
# Input-Monitoring status (`IOHIDCheckAccess`) with typed request/access enums —
# see platform/permissions.rs.
objc2-io-kit = { workspace = true, features = ["std", "hidsystem"] }
# NSEvent global-monitor handlers are ObjC blocks (overlay click-away dismissal).
block2 = { workspace = true }

# unsafe_code stays denied (from [workspace.lints]); the status-item/tray modules
# opt in locally with #[expect(unsafe_code)] for the few objc2 calls
# (init-with-action, set-target, super-init) that remain unsafe.
[lints]
workspace = true

[dev-dependencies]
gpui = { workspace = true, features = ["test-support"] }
tempfile = "3.27.0"

[package.metadata.bundle]
name = "OpenLogi"
identifier = "org.openlogi.openlogi"
# No `version` override — cargo-bundle falls back to the crate version, which
# is workspace-unified. A literal here goes stale the moment release-plz bumps.
category = "public.app-category.utilities"
short_description = "Lightweight local-first companion for Logitech HID++ peripherals."
long_description = """\
OpenLogi talks to Logitech HID++ mice and keyboards directly — no \
cloud account, no telemetry, no auto-update poller. Plain TOML config, \
single binary; device renders are fetched on demand on first launch.
"""
osx_minimum_system_version = "13.0"
# Deep-link scheme for the release app. The dev build is hand-bundled, so its
# dev/Info.plist declares the same scheme separately — keep the two in sync.
osx_url_schemes = ["openlogi"]
icon = ["icon/AppIcon.icns"]
resources = ["assets/**/*"]
